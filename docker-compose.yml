services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=change-me
      - DATABASE_URL=sqlite+aiosqlite:///./app.db
      - DEBUG=false
    volumes:
      - ./data:/app/data
